package sort

//BubbleSort ...
func BubbleSort(list []int) {
	for i := range list {
		for j := 0; j < len(list)-1-i; j++ {
			if list[j] > list[j+1] {
				list[j], list[j+1] = list[j+1], list[j]
			}
		}
	}
}
